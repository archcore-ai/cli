---
title: "UI Kit — Overview"
status: accepted
tags: [frontend]
---

## Overview

The shared component library used by the apps. As a `packages/*` project it must
not depend on any app (see the root **Dependency Boundaries** rule, mounted as a
global). It exposes components built on the design tokens.
