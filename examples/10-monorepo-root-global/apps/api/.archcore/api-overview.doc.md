---
title: "API Service — Overview"
status: accepted
---

## Overview

The Go backend API. Like every project in this monorepo, it mounts the root
`.archcore/` as a global — the shared conventions and dependency boundaries are
inherited, not redefined here.
