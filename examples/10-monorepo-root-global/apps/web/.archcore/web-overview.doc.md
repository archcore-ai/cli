---
title: "Web App — Overview"
status: accepted
---

## Overview

The customer-facing React SPA. It consumes the shared `ui` package and mounts
the monorepo root `.archcore/` as a read-only global, so the repo-wide
conventions and dependency rules apply here automatically.

Local docs cover only web-specific concerns.
