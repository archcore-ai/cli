---
title: "Coding Standards (vendored)"
status: accepted
---

## Rule

Vendored copy of the company coding baseline: wrap errors with context, log
structured JSON, version APIs by URL prefix. Pinned into this repo so the
standard travels with the code.

## Note

This document lives under `.archcore/global/company/` and is mounted read-only.
The `global/` directory is **reserved** — skipped by the local scan and visible
only because it is declared in `settings.json`.
