---
title: "Web App (monorepo) — Overview"
status: accepted
---

## Overview

The customer web frontend, one app in this monorepo. It mounts the shared
`shared-standards` global (monorepo conventions, release process) from
`packages/shared-standards`. Open your agent from this directory (`apps/web`) to
see local docs plus the shared standards as read-only.
