---
title: "API Service (monorepo) — Overview"
status: accepted
---

## Overview

The backend API, the second app in this monorepo. It mounts the **same** shared
`shared-standards` global as `apps/web` — one source, two consumers. Open your
agent from `apps/api` to see this service's docs plus the shared standards
read-only.
